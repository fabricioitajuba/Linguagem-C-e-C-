# Comando em C++

- Para compilar:

$ g++ -o comando main.cpp

- Para testar:

$ ./comando -a
Argumento -a

$ ./comando -b
Argumento -b

$ ./comando -c
Argumento -c

$ ./comando -d
Nenhum argumento válido passado!